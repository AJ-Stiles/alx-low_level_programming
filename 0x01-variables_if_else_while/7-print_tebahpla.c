#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 *  * main - Entry
 *   * Return: Always 0
 */
{
		char ch;

			for (ch = 'z'; ch >= 'a'; ch--)
					{
								putchar(ch);
									}
				putchar('\n');
					return (0);
}

